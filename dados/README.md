# Pasta `dados/`

Drop zone pra arquivos que você quer analisar (CSV, XLSX, TXT, PDF, JSON).

Use com `/analisar-dados dados/seu-arquivo.csv` ou apenas peça pro Claude analisar.

Arquivos nessa pasta não vão pro GitHub (estão no `.gitignore`).
